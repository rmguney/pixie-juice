[pytest]
minversion = 8.0
addopts = --strict-markers --strict-config --verbose --tb=short --asyncio-mode=auto
testpaths = .
asyncio_mode = auto
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    browser_automation: marks tests as browser automation tests
